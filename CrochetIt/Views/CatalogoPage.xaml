<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrochetIt.Views.CatalogoPage"
             Title="Catálogo">
    <CollectionView x:Name="CatalogoCollection">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="2"/>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout>
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnPatronTapped"/>
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="Subtitle" Margin="20,20,20,20"/>
                    <Image Source="{Binding ImageUrl}" Margin="10,10,10,10"/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>